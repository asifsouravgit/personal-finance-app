<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asifahmed.mainpersonalfinanceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LedgerActivity"></activity>
        <activity android:name=".ExpenseAddActivity" />
        <activity android:name=".IncomeAddActivity" />
        <activity android:name=".AccountUpdateDelete" />
        <activity android:name=".AccountAdd" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".CatagoryUpdateDeleteActivity" />
        <activity android:name=".CategoryAddActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>